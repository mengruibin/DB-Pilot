# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Agent Framework
langgraph>=0.3.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0

# Checkpointer (PostgreSQL persistence)
langgraph-checkpoint-postgres>=3.1.0
# psycopg 二进制模式：自带 libpq>=10，避免 SCRAM 认证失败（PG14+ 默认 SCRAM）
psycopg[binary]>=3.3.0

# ORM & Data
sqlalchemy>=2.0.0
pydantic>=2.0
pydantic-settings>=2.0

# SQL Parser (multi-dialect, security audit)
sqlglot>=25.0.0

# Async HTTP Client (per AGENTS.md §禁止项)
httpx>=0.27.0

# Configuration
python-dotenv>=1.0.0

# Async SQLite Driver (dev/testing)
aiosqlite>=0.20.0
# Async MySQL Driver (production)
aiomysql>=0.2.0

# DB Migration
alembic>=1.13.0

# Dev
ruff>=0.5.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Observability
structlog>=24.0.0

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
